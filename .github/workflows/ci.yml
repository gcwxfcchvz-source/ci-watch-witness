name: ci
on:
  pull_request:
    paths: ['ci-src/**']
  push:
    branches: [main]
    paths: ['ci-src/**']
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash run.sh
